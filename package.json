{
  "type": "module",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "e2e"
  ],
  "scripts": {
    "build:client": "yarn workspace nextflix-client build",
    "dev": "yarn workspace nextflix-server dev",
    "dev:debug": "yarn workspace nextflix-server dev:debug",
    "heroku-postbuild": "yarn workspace nextflix-client build",
    "start": "yarn workspace nextflix-server start",
    "dev:cypress": "yarn workspace nextflix-server dev:e2e",
    "e2e:open": "yarn workspace nextflix-e2e e2e:open",
    "e2e:run": "yarn workspace nextflix-e2e e2e:run"
  },
  "engines": {
    "node": "^14.15"
  }
}
